import {createStore as _createStore} from 'vuex';

export function createStore() {
    return _createStore({
        state: {

        },
        mutations: {
            
        },
        // "strict" is a learning tool, never appears in production code
        strict: true
    })
}